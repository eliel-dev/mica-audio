# Referencia - HTTP API v1

Campos de metadados relevantes para devices HUB75:

- `BoardModel` (ex.: `esp32s3_devkitc1`)
- `PanelType` (ex.: `hub75_p2_5_128x64_smd2121_scan32`)

O shape do payload nao mudou. O valor canonico mudou para o painel 128x64.
