<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MicaAudio" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.MicaAudio.Splash" parent="Theme.MicaAudio">
        <!-- Splash screen will use the Compose theme once loaded -->
    </style>
</resources>
